---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="ru">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<style>
			.hero-section {
				text-align: center;
				padding: 4rem 1rem;
				display: flex;
				flex-direction: column;
				align-items: center;
				gap: 1.5rem;
			}
			.tagline {
				font-family: 'Fira Code', monospace;
				color: var(--accent);
				letter-spacing: 0.1em;
				text-transform: uppercase;
				font-size: 0.9rem;
			}
			h1 {
				font-size: 4rem;
				line-height: 1.1;
				margin: 0;
				max-width: 800px;
			}
			.subtitle {
				font-size: 1.25rem;
				color: var(--text-secondary);
				max-width: 600px;
				margin: 0 auto;
			}
			.cta-buttons {
				display: flex;
				gap: 1rem;
				margin-top: 2rem;
			}
			.btn {
				padding: 1rem 2rem;
				border-radius: var(--radius);
				font-weight: 600;
				text-decoration: none;
				transition: var(--transition);
			}
			.btn-primary {
				background: var(--accent);
				color: #000;
				box-shadow: var(--glow-accent);
			}
			.btn-primary:hover {
				background: #fff;
				color: #000;
				box-shadow: 0 0 40px rgba(255, 255, 255, 0.6);
			}
			.btn-secondary {
				background: var(--bg-surface);
				border: var(--glass-border);
				color: #fff;
			}
			.btn-secondary:hover {
				background: var(--bg-surface-solid);
				border-color: rgba(255,255,255,0.2);
			}
			
			.features {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
				gap: 2rem;
				margin-top: 4rem;
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section class="hero-section">
				<span class="tagline">Alternative Physics</span>
				<h1><span class="gradient-text">Ether Dynamics</span> & Matter Structure</h1>
				<p class="subtitle">
					Exploring how our world works at the fundamental level. Forget probabilities—welcome to the engineering approach to atomic structures.
				</p>
				<div class="cta-buttons">
					<a href="/blog/atom-structure-part-1" class="btn btn-primary">Start Reading</a>
					<a href="/blog" class="btn btn-secondary">All Articles</a>
				</div>
			</section>
			
			<section class="features">
				<div class="glass-panel">
					<h3>🔭 Direct Observations</h3>
					<p>We analyze real images of atoms obtained by quantum microscopes, not mathematical abstractions.</p>
				</div>
				<div class="glass-panel">
					<h3>🌪️ Vortex Theory</h3>
					<p>The Ether returns. Discover why matter is a stable toroidal vortex in a superdense medium.</p>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
